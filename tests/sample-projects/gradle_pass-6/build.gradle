plugins {
    id 'java'
}

gradle.projectsEvaluated {
    def javaVersion = System.getProperty('java.version')
    if (!javaVersion.startsWith('1.6')) {
        throw new GradleException("This build requires Java 6, but found version: ${javaVersion}")
    }
}
